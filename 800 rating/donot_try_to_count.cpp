#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main (){
    int t;
    cin >> t;
    while ( t-- ){
        int n,m;
        cin >> n >> m;
        string x;
        cin >> x; // N is the length of the string x
        string s;
        cin >> s; // M is the length of the string s
        // Implemented in Python
    }
}